# ENGLALAND static redesign

Local responsive static copy of `https://www.englaland.com/`.

## Run locally

```bash
python3 -m http.server 4173
```

Open `http://localhost:4173/index.html`.

## Editing

The HTML files in this directory are the source of truth. Edit pages and `assets/css/site.css` directly.
